# Prompt Engineering & Execution Log

This document records the user prompts, instructions, rationale, and execution phases across the development of the Sezzle Full-Stack Calculator starting from Milestone 1.

### Prompt 1
**Prompt:** Run the /initialize-context workflow, then execute the following 3 atomic setup steps for Milestone 1: Step 1: Create the `api/` directory and initialize `api/package.json` with TypeScript and test tooling: - Dependencies: `zod` (for upcoming schema validation). - DevDependencies: `typescript`, `@types/node`, `vitest`, `@vitest/coverage-v8`. - Scripts: `"build": "tsc"`, `"test": "vitest run"`, `"test:coverage": "vitest run --coverage"`. Step 2: Create `api/tsconfig.json` configured for strict TypeScript (ES2022/NodeNext, strict mode enabled, noImplicitAny, outDir `dist`). Step 3: Add a temporary sanity test file `api/src/sanity.test.ts` with a simple assertion, run `npm test` inside `api/` to verify the runner works, then remove the sanity file. Ensure you update `PROMPT_LOG.md` with: - The exact prompt text. - A concise 1-2 sentence summary of actions taken. Do not write the calculator domain logic yet. Pause and report back with the test runner output once verified.
**Action Taken:** Scaffolded the backend `api/` workspace by initializing `package.json` with required dependencies (zod, typescript, vitest) and configuring `tsconfig.json` for strict TypeScript. Successfully ran and then removed a temporary sanity test to verify the test runner.

### Prompt 2
**Prompt:** Please execute the following 3 atomic steps: Step 1: Stage and commit the previous changes from Prompt 1: - Stage `api/` workspace files, `PROMPT_LOG.md`, and `.agents/logs/`. - Commit with the message: `chore(api): scaffold api package, tsconfig, and vitest runner`. Step 2: Review `.agents/rules/rules.md` to ensure strict alignment with our zero-`any` TypeScript policy, English conventions, and error handling patterns. Step 3: Implement the pure domain calculation module in `api/src/domain/calculator.ts`: - Define custom domain error classes: `DivisionByZeroError` and `InvalidOperandError` (extending `Error` with a distinct `code` property). - Implement a precision helper `normalizePrecision(value: number): number` bounding results to a deterministic 10 decimal places to eliminate IEEE 754 floating-point artifacts (e.g., `0.1 + 0.2 === 0.3`). - Implement and export pure functions: - Binary: `add(a, b)`, `subtract(a, b)`, `multiply(a, b)`, `divide(a, b)`, `power(a, b)` - Unary: `sqrt(a)`, `percentage(a)` (where percentage evaluates `a / 100`) - Guardrails: throw `DivisionByZeroError` if `b === 0` in `divide`; throw `InvalidOperandError` if `a < 0` in `sqrt`; guard against `NaN` or infinite outputs. - Keep the module completely pure with no HTTP or framework dependencies. Log this prompt in `PROMPT_LOG.md` along with a 1-2 sentence summary of actions taken, and update `.agents/logs/`. Do not write tests or commit Step 3 yet. Pause and report back for review.
**Action Taken:** Committed the previous scaffold changes. Implemented the pure domain arithmetic module `api/src/domain/calculator.ts` with strict typing, custom errors (`DivisionByZeroError`, `InvalidOperandError`), and IEEE 754 precision normalization.

### Prompt 3
**Prompt:** Please execute the following 2 atomic steps: Step 1: Stage and commit the domain logic implementation: - Stage `api/src/domain/calculator.ts`, `PROMPT_LOG.md`, and `.agents/logs/`. - Commit with the message: `feat(api): implement calculator domain operations and precision normalization`. Step 2: Implement comprehensive, table-driven unit tests in `api/tests/calculator.test.ts` and run the test suite: - Use Vitest's `describe` and `test.each` to construct table-driven test suites covering: - Standard operations for all functions (`add`, `subtract`, `multiply`, `divide`, `power`, `sqrt`, `percentage`). - IEEE 754 precision edge cases (e.g., `0.1 + 0.2 === 0.3`, `0.7 * 0.1 === 0.07`, `0.3 - 0.1 === 0.2`). - Error guardrails: `divide` with zero throws `DivisionByZeroError`, `sqrt` with negative numbers throws `InvalidOperandError`, and infinite outputs (overflow) throw `InvalidOperandError`. - Boundary and identity cases (zeros, negative inputs, large numbers, fractional exponents). - Run the test suite with coverage enabled (`npm run test:coverage` inside `api/`) to verify >90% code coverage across `api/src/domain/calculator.ts`. Log this prompt in `PROMPT_LOG.md` along with a 1-2 sentence summary of actions taken, and update `.agents/logs/`. Pause and report the test and coverage outputs for review before committing Step 2.
**Action Taken:** Committed the domain logic implementation from the previous step. Authored table-driven unit tests in `api/tests/calculator.test.ts` verifying all arithmetic edge cases and guardrails, achieving 100% test coverage on the domain module.

### Prompt 4
**Prompt:** Please execute this single atomic step: Stage and commit the unit tests and associated logs: - Stage `api/tests/calculator.test.ts`, `PROMPT_LOG.md`, and `.agents/logs/`. - Commit with the message: `test(api): add table-driven unit tests for domain calculator operations and precision`. Do not scaffold Milestone 2 yet. Pause and report back with git status once committed.
**Action Taken:** Committed the domain logic unit tests and updated session logs.

### Prompt 5
**Prompt:** Please execute the following 3 atomic steps for Milestone 2: Step 1: Install Express and HTTP development tooling inside `api/`: - Dependencies: `express`, `cors` - DevDependencies: `@types/express`, `@types/cors`, `supertest`, `@types/supertest`, `tsx` - Add to `api/package.json` scripts: `"dev": "tsx watch src/server.ts"` Step 2: Create validation schemas and centralized error handling: - `api/src/schemas/calculator.schema.ts`: Define Zod schemas for `binaryOperationSchema` (requiring numeric `a` and `b`) and `unaryOperationSchema` (requiring numeric `a`, `b` optional/omitted). - `api/src/middleware/errorHandler.ts`: Express error middleware catching: - Zod validation errors -> HTTP 400 with descriptive error details. - Domain errors (`DivisionByZeroError`, `InvalidOperandError`) -> HTTP 400 with `{ error: err.code, message: err.message }`. - Malformed JSON body parser errors -> HTTP 400. - Fallback unhandled errors -> HTTP 500. Step 3: Scaffold Express application and dedicated routes: - `api/src/routes/v1/calculator.routes.ts`: Implement POST routes for `/add`, `/subtract`, `/multiply`, `/divide`, `/power`, `/sqrt`, and `/percentage` calling the domain calculator functions. - `api/src/app.ts`: Configure Express app with `cors()`, `express.json()`, mount routes under `/api/v1`, and attach the error handler. Export `app` without starting the listener (to support supertest). - `api/src/server.ts`: Import `app` and listen on `PORT || 3001`. Log this prompt in `PROMPT_LOG.md` with a 1-2 sentence summary of actions taken, and update `.agents/logs/`. Do not write integration tests or commit yet. Pause and report back for review.
**Action Taken:** Installed Express and related tooling. Scaffolded the Express server application including Zod payload validation schemas, centralized error middleware, and the `/api/v1/` route handlers for all math operations.

### Prompt 6
**Prompt:** Task approved, stage and commit changes with an appropriate comment.
**Action Taken:** Staged and committed the Express backend scaffolding, including the application setup, Zod validation schemas, and route handlers.

### Prompt 7
**Prompt:** Please execute the following tasks to complete Milestone 2: 1. Create table-driven integration tests in `api/tests/integration.test.ts` using `supertest`: - Test all 7 endpoints (`/api/v1/add`, `/subtract`, `/multiply`, `/divide`, `/power`, `/sqrt`, `/percentage`). - Happy path: Verify correct calculation results and HTTP 200 responses. - Validation errors: Test missing fields, non-numeric values, and invalid JSON payloads, asserting HTTP 400 with descriptive error bodies. - Domain errors: Test division by zero (`/divide` with `b: 0`) and negative square root (`/sqrt` with negative `a`), asserting HTTP 400 and matching error codes (`DIVISION_BY_ZERO`, `INVALID_OPERAND`). 2. Run the test suite with coverage (`npm run test:coverage` inside `api/`) to verify all routes, schemas, and middleware pass with high coverage. 3. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not commit yet. Pause and report the test execution and coverage output for review.
**Action Taken:** Authored table-driven integration tests for the Express endpoints using Supertest. Ran the test suite successfully and verified over 96% coverage across all application routes and middleware.

### Prompt 8
**Prompt:** Task approved, stage and commit changes with an appropriate comment.
**Action Taken:** Staged and committed the integration test suite.

### Prompt 9
**Prompt:** Please execute the following 3 atomic steps for Milestone 3: Step 1: Initialize the frontend application inside the root `web/` directory using Vite with React and TypeScript: - Dependencies: `react`, `react-dom`, `lucide-react` (for clean UI toggle/status icons). - DevDependencies: `typescript`, `@types/react`, `@types/react-dom`, `@vitejs/plugin-react`, `tailwindcss`, `postcss`, `autoprefixer`, `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`. Step 2: Configure styles and test tooling: - Initialize and configure `web/tailwind.config.js` with `darkMode: 'class'` and content paths for `src/**/*.{ts,tsx}`. - Configure `web/src/index.css` with `@tailwind base; @tailwind components; @tailwind utilities;`. - Configure `web/vite.config.ts` with test environment set to `jsdom` and setup file `src/test/setup.ts` (importing `@testing-library/jest-dom`). - Add scripts in `web/package.json`: `"dev": "vite"`, `"build": "tsc && vite build"`, `"test": "vitest run"`, `"test:coverage": "vitest run --coverage"`. Step 3: Create a minimal smoke test in `web/src/App.test.tsx` verifying that a dummy `<App />` component mounts, run `npm test` inside `web/` to confirm the testing pipeline passes, then remove the dummy test. Record this prompt and a 1-2 sentence summary of actions in `PROMPT_LOG.md`, and update `.agents/logs/`. Do not commit or build the state machine yet. Pause and report back with the test runner output.
**Action Taken:** Scaffolded the React frontend by initializing the Vite application, configuring Tailwind CSS, and setting up the testing pipeline with Vitest, JSDOM, and React Testing Library. We ran a dummy component test to verify the test setup.

### Prompt 10
**Prompt:** Please execute this atomic cleanup step before we proceed to the state machine: 1. Fix TypeScript config in `web/`: - In `web/vite.config.ts`, import `defineConfig` from `vitest/config` to resolve the type mismatch on the `test` property. - In `web/tsconfig.app.json`, explicitly exclude test files and setup: add `"exclude": ["src/**/*.test.ts", "src/**/*.test.tsx", "src/test/**"]`. - Ensure `web/tsconfig.json` or a test tsconfig references Vitest types so tests stay fully typed. 2. Clean up workspace structure: - Delete the redundant `web/.gitignore`. - Ensure the root `.gitignore` ignores `web/node_modules/`, `web/dist/`, and coverage outputs. 3. Verify: - Run `npx tsc -b` inside `web/` to confirm 0 TypeScript errors. 4. Stage all scaffolding and cleanup changes, and commit with: `chore(web): scaffold vite react tailwind app with vitest pipeline` Update PROMPT_LOG.md and .agents/logs/. Pause and report back with git status once committed.
**Action Taken:** Fixed Vite configuration typing, configured `tsconfig.test.json` to properly type-check tests while excluding them from the app build, deleted the nested `.gitignore`, successfully verified the types with `npx tsc -b`, and committed the initialized frontend.

### Prompt 11
**Prompt:** Please execute the following 2 atomic steps for Milestone 3: Step 1: Implement the calculator accumulator state machine in `web/src/hooks/useCalculator.ts`: - Define explicit types for operations: Binary (`'add' | 'subtract' | 'multiply' | 'divide' | 'power'`) and Unary (`'sqrt' | 'percentage'`). - Manage state: - `display`: Current string shown on the primary display (default `'0'`). - `accumulator`: Previous numeric value stored when chaining binary operations. - `operation`: Active binary operation, if pending. - `waitingForOperand`: Boolean flag indicating whether the next digit should replace the display. - `error`: Error message string or null. - Expose state and action handlers: - `inputDigit(digit: string)`: Handles digit buffering, prevents leading zero stacks, replaces buffer if `waitingForOperand` is true. - `inputDecimal()`: Guards against multiple decimals; prepends `'0.'` if waiting for operand. - `clear()`: Resets display to `'0'` (C behavior). - `allClear()`: Resets all state (AC behavior: display, accumulator, operation, error). - `toggleSign()`: Flips sign of current display value without NaN bugs. - `setBinaryOperation(op: BinaryOperation)`: Stores accumulator and operation; if chaining consecutively, computes intermediate local result before setting new operator. - `applyUnaryOperation(op: UnaryOperation)`: Immediately calculates unary transformation on the active display value locally (using IEEE 754 precision normalization) and updates display. - `evaluate()`: If binary operation and accumulator exist, computes the final result, updates display, clears pending operation, and sets `waitingForOperand = true`. Step 2: Implement comprehensive unit tests for the state machine in `web/src/hooks/useCalculator.test.ts`: - Use Vitest and `@testing-library/react`'s `renderHook` and `act`. - Test suites covering: - Digit input and buffering (e.g. typing `1`, `2`, `3` gives `'123'`). - Decimal guard (preventing `1.2.3`). - Sign toggling (`5` -> `-5` -> `5`, `0` -> `0`). - Clear (`C`) vs All Clear (`AC`). - Binary chaining (e.g. `5 + 3 = 8`, `5 + 3 * 2` local sequence). - Unary execution (e.g. `16` -> `sqrt` -> `4`, `50` -> `%` -> `0.5`). - Error boundaries (e.g. divide by zero local handling, negative sqrt). - Run `npm test` inside `web/` to confirm all hook tests pass. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not build UI components or commit yet. Pause and report the test execution results for review.
**Action Taken:** Implemented the `useCalculator` state machine hook with precision normalization, local evaluation, and error boundary handling. Created a comprehensive Vitest test suite using React Testing Library's `renderHook` to verify digit buffering, chained evaluations, unary transforms, and edge cases.

### Prompt 12
**Prompt:** Please execute this atomic step: 1. In `web/src/hooks/useCalculator.ts`, eliminate all `any` types in `catch` blocks (replace with `catch (err: unknown)` and narrow with `err instanceof Error ? err.message : 'UNKNOWN_ERROR'`) to maintain our zero-any policy. 2. Run `npm test` inside `web/` to confirm all 8 hook tests pass with 0 errors. 3. Task approved: stage and commit the hook, tests, and updated logs with an appropriate commit message. Pause and report back with git status once committed.
**Action Taken:** Eliminated all explicit `any` types from the `useCalculator` catch blocks, maintaining strict TypeScript safety. Verified 100% test passing rate and committed the state machine hook and unit tests.

### Prompt 13
**Prompt:** Please review `ARCHITECTURE.md` and `.agents/rules.md` to refresh architectural constraints (zero `any`, strict typing, component modularity). Next, implement the presentational UI components inside `web/src/components/` with Tailwind CSS styling: 1. `web/src/components/ThemeToggle.tsx`: Toggle button controlling dark/light mode. 2. `web/src/components/Display.tsx`: Displays current value and expression cleanly. 3. `web/src/components/Keypad.tsx`: Renders the keypad grid with varying styles. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not assemble `App.tsx` or run integration tests yet. Pause and report when components are created.
**Action Taken:** Authored the `ThemeToggle`, `Display`, and `Keypad` presentational React components inside `web/src/components/`. Used dynamic Tailwind CSS classes for premium styling (glassmorphism, micro-animations, dark mode) and properly passed strict types to all props.

### Prompt 14
**Prompt:** Please execute the following 3 atomic steps to complete Milestone 3: Step 1: Assemble the main layout in `web/src/App.tsx`: - Centered card layout matching our concept. Step 2: Add keyboard interaction: - Implement a global `keydown` event listener mapping keys to `useCalculator` actions. Step 3: Author UI interaction tests in `web/src/App.test.tsx` using Vitest and React Testing Library. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not commit yet. Pause and report back with the test results.
**Action Taken:** Assembled `App.tsx` integrating the UI components with the `useCalculator` hook and wired up comprehensive keyboard event listeners. Authored component integration tests verifying both mouse and keyboard interactions, resulting in a 100% test pass rate.
