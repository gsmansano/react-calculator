# Prompt Engineering & Execution Log

This document records the user prompts, instructions, rationale, and execution phases across the development of the Sezzle Full-Stack Calculator starting from Milestone 1.

### Prompt 1
**Prompt:** Run the /initialize-context workflow, then execute the following 3 atomic setup steps for Milestone 1: Step 1: Create the `api/` directory and initialize `api/package.json` with TypeScript and test tooling: - Dependencies: `zod` (for upcoming schema validation). - DevDependencies: `typescript`, `@types/node`, `vitest`, `@vitest/coverage-v8`. - Scripts: `"build": "tsc"`, `"test": "vitest run"`, `"test:coverage": "vitest run --coverage"`. Step 2: Create `api/tsconfig.json` configured for strict TypeScript (ES2022/NodeNext, strict mode enabled, noImplicitAny, outDir `dist`). Step 3: Add a temporary sanity test file `api/src/sanity.test.ts` with a simple assertion, run `npm test` inside `api/` to verify the runner works, then remove the sanity file. Ensure you update `PROMPT_LOG.md` with: - The exact prompt text. - A concise 1-2 sentence summary of actions taken. Do not write the calculator domain logic yet. Pause and report back with the test runner output once verified.
**Action Taken:** Scaffolded the backend `api/` workspace by initializing `package.json` with required dependencies (zod, typescript, vitest) and configuring `tsconfig.json` for strict TypeScript. Successfully ran and then removed a temporary sanity test to verify the test runner.

### Prompt 2
**Prompt:** Please execute the following 3 atomic steps: Step 1: Stage and commit the previous changes from Prompt 1: - Stage `api/` workspace files, `PROMPT_LOG.md`, and `.agents/logs/`. - Commit with the message: `chore(api): scaffold api package, tsconfig, and vitest runner`. Step 2: Review `.agents/rules/rules.md` to ensure strict alignment with our zero-`any` TypeScript policy, English conventions, and error handling patterns. Step 3: Implement the pure domain calculation module in `api/src/domain/calculator.ts`: - Define custom domain error classes: `DivisionByZeroError` and `InvalidOperandError` (extending `Error` with a distinct `code` property). - Implement a precision helper `normalizePrecision(value: number): number` bounding results to a deterministic 10 decimal places to eliminate IEEE 754 floating-point artifacts (e.g., `0.1 + 0.2 === 0.3`). - Implement and export pure functions: - Binary: `add(a, b)`, `subtract(a, b)`, `multiply(a, b)`, `divide(a, b)`, `power(a, b)` - Unary: `sqrt(a)`, `percentage(a)` (where percentage evaluates `a / 100`) - Guardrails: throw `DivisionByZeroError` if `b === 0` in `divide`; throw `InvalidOperandError` if `a < 0` in `sqrt`; guard against `NaN` or infinite outputs. - Keep the module completely pure with no HTTP or framework dependencies. Log this prompt in `PROMPT_LOG.md` along with a 1-2 sentence summary of actions taken, and update `.agents/logs/`. Do not write tests or commit Step 3 yet. Pause and report back for review.
**Action Taken:** Committed the previous scaffold changes. Implemented the pure domain arithmetic module `api/src/domain/calculator.ts` with strict typing, custom errors (`DivisionByZeroError`, `InvalidOperandError`), and IEEE 754 precision normalization.

### Prompt 3
**Prompt:** Please execute the following 2 atomic steps: Step 1: Stage and commit the domain logic implementation: - Stage `api/src/domain/calculator.ts`, `PROMPT_LOG.md`, and `.agents/logs/`. - Commit with the message: `feat(api): implement calculator domain operations and precision normalization`. Step 2: Implement comprehensive, table-driven unit tests in `api/tests/calculator.test.ts` and run the test suite: - Use Vitest's `describe` and `test.each` to construct table-driven test suites covering: - Standard operations for all functions (`add`, `subtract`, `multiply`, `divide`, `power`, `sqrt`, `percentage`). - IEEE 754 precision edge cases (e.g., `0.1 + 0.2 === 0.3`, `0.7 * 0.1 === 0.07`, `0.3 - 0.1 === 0.2`). - Error guardrails: `divide` with zero throws `DivisionByZeroError`, `sqrt` with negative numbers throws `InvalidOperandError`, and infinite outputs (overflow) throw `InvalidOperandError`. - Boundary and identity cases (zeros, negative inputs, large numbers, fractional exponents). - Run the test suite with coverage enabled (`npm run test:coverage` inside `api/`) to verify >90% code coverage across `api/src/domain/calculator.ts`. Log this prompt in `PROMPT_LOG.md` along with a 1-2 sentence summary of actions taken, and update `.agents/logs/`. Pause and report the test and coverage outputs for review before committing Step 2.
**Action Taken:** Committed the domain logic implementation from the previous step. Authored table-driven unit tests in `api/tests/calculator.test.ts` verifying all arithmetic edge cases and guardrails, achieving 100% test coverage on the domain module.

### Prompt 4
**Prompt:** Please execute this single atomic step: Stage and commit the unit tests and associated logs: - Stage `api/tests/calculator.test.ts`, `PROMPT_LOG.md`, and `.agents/logs/`. - Commit with the message: `test(api): add table-driven unit tests for domain calculator operations and precision`. Do not scaffold Milestone 2 yet. Pause and report back with git status once committed.
**Action Taken:** Committed the domain logic unit tests and updated session logs.

### Prompt 5
**Prompt:** Please execute the following 3 atomic steps for Milestone 2: Step 1: Install Express and HTTP development tooling inside `api/`: - Dependencies: `express`, `cors` - DevDependencies: `@types/express`, `@types/cors`, `supertest`, `@types/supertest`, `tsx` - Add to `api/package.json` scripts: `"dev": "tsx watch src/server.ts"` Step 2: Create validation schemas and centralized error handling: - `api/src/schemas/calculator.schema.ts`: Define Zod schemas for `binaryOperationSchema` (requiring numeric `a` and `b`) and `unaryOperationSchema` (requiring numeric `a`, `b` optional/omitted). - `api/src/middleware/errorHandler.ts`: Express error middleware catching: - Zod validation errors -> HTTP 400 with descriptive error details. - Domain errors (`DivisionByZeroError`, `InvalidOperandError`) -> HTTP 400 with `{ error: err.code, message: err.message }`. - Malformed JSON body parser errors -> HTTP 400. - Fallback unhandled errors -> HTTP 500. Step 3: Scaffold Express application and dedicated routes: - `api/src/routes/v1/calculator.routes.ts`: Implement POST routes for `/add`, `/subtract`, `/multiply`, `/divide`, `/power`, `/sqrt`, and `/percentage` calling the domain calculator functions. - `api/src/app.ts`: Configure Express app with `cors()`, `express.json()`, mount routes under `/api/v1`, and attach the error handler. Export `app` without starting the listener (to support supertest). - `api/src/server.ts`: Import `app` and listen on `PORT || 3001`. Log this prompt in `PROMPT_LOG.md` with a 1-2 sentence summary of actions taken, and update `.agents/logs/`. Do not write integration tests or commit yet. Pause and report back for review.
**Action Taken:** Installed Express and related tooling. Scaffolded the Express server application including Zod payload validation schemas, centralized error middleware, and the `/api/v1/` route handlers for all math operations.

### Prompt 6
**Prompt:** Task approved, stage and commit changes with an appropriate comment.
**Action Taken:** Staged and committed the Express backend scaffolding, including the application setup, Zod validation schemas, and route handlers.

### Prompt 7
**Prompt:** Please execute the following tasks to complete Milestone 2: 1. Create table-driven integration tests in `api/tests/integration.test.ts` using `supertest`: - Test all 7 endpoints (`/api/v1/add`, `/subtract`, `/multiply`, `/divide`, `/power`, `/sqrt`, `/percentage`). - Happy path: Verify correct calculation results and HTTP 200 responses. - Validation errors: Test missing fields, non-numeric values, and invalid JSON payloads, asserting HTTP 400 with descriptive error bodies. - Domain errors: Test division by zero (`/divide` with `b: 0`) and negative square root (`/sqrt` with negative `a`), asserting HTTP 400 and matching error codes (`DIVISION_BY_ZERO`, `INVALID_OPERAND`). 2. Run the test suite with coverage (`npm run test:coverage` inside `api/`) to verify all routes, schemas, and middleware pass with high coverage. 3. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not commit yet. Pause and report the test execution and coverage output for review.
**Action Taken:** Authored table-driven integration tests for the Express endpoints using Supertest. Ran the test suite successfully and verified over 96% coverage across all application routes and middleware.

### Prompt 8
**Prompt:** Task approved, stage and commit changes with an appropriate comment.
**Action Taken:** Staged and committed the integration test suite.

### Prompt 9
**Prompt:** Please execute the following 3 atomic steps for Milestone 3: Step 1: Initialize the frontend application inside the root `web/` directory using Vite with React and TypeScript: - Dependencies: `react`, `react-dom`, `lucide-react` (for clean UI toggle/status icons). - DevDependencies: `typescript`, `@types/react`, `@types/react-dom`, `@vitejs/plugin-react`, `tailwindcss`, `postcss`, `autoprefixer`, `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`. Step 2: Configure styles and test tooling: - Initialize and configure `web/tailwind.config.js` with `darkMode: 'class'` and content paths for `src/**/*.{ts,tsx}`. - Configure `web/src/index.css` with `@tailwind base; @tailwind components; @tailwind utilities;`. - Configure `web/vite.config.ts` with test environment set to `jsdom` and setup file `src/test/setup.ts` (importing `@testing-library/jest-dom`). - Add scripts in `web/package.json`: `"dev": "vite"`, `"build": "tsc && vite build"`, `"test": "vitest run"`, `"test:coverage": "vitest run --coverage"`. Step 3: Create a minimal smoke test in `web/src/App.test.tsx` verifying that a dummy `<App />` component mounts, run `npm test` inside `web/` to confirm the testing pipeline passes, then remove the dummy test. Record this prompt and a 1-2 sentence summary of actions in `PROMPT_LOG.md`, and update `.agents/logs/`. Do not commit or build the state machine yet. Pause and report back with the test runner output.
**Action Taken:** Scaffolded the React frontend by initializing the Vite application, configuring Tailwind CSS, and setting up the testing pipeline with Vitest, JSDOM, and React Testing Library. We ran a dummy component test to verify the test setup.

### Prompt 10
**Prompt:** Please execute this atomic cleanup step before we proceed to the state machine: 1. Fix TypeScript config in `web/`: - In `web/vite.config.ts`, import `defineConfig` from `vitest/config` to resolve the type mismatch on the `test` property. - In `web/tsconfig.app.json`, explicitly exclude test files and setup: add `"exclude": ["src/**/*.test.ts", "src/**/*.test.tsx", "src/test/**"]`. - Ensure `web/tsconfig.json` or a test tsconfig references Vitest types so tests stay fully typed. 2. Clean up workspace structure: - Delete the redundant `web/.gitignore`. - Ensure the root `.gitignore` ignores `web/node_modules/`, `web/dist/`, and coverage outputs. 3. Verify: - Run `npx tsc -b` inside `web/` to confirm 0 TypeScript errors. 4. Stage all scaffolding and cleanup changes, and commit with: `chore(web): scaffold vite react tailwind app with vitest pipeline` Update PROMPT_LOG.md and .agents/logs/. Pause and report back with git status once committed.
**Action Taken:** Fixed Vite configuration typing, configured `tsconfig.test.json` to properly type-check tests while excluding them from the app build, deleted the nested `.gitignore`, successfully verified the types with `npx tsc -b`, and committed the initialized frontend.

### Prompt 11
**Prompt:** Please execute the following 2 atomic steps for Milestone 3: Step 1: Implement the calculator accumulator state machine in `web/src/hooks/useCalculator.ts`: - Define explicit types for operations: Binary (`'add' | 'subtract' | 'multiply' | 'divide' | 'power'`) and Unary (`'sqrt' | 'percentage'`). - Manage state: - `display`: Current string shown on the primary display (default `'0'`). - `accumulator`: Previous numeric value stored when chaining binary operations. - `operation`: Active binary operation, if pending. - `waitingForOperand`: Boolean flag indicating whether the next digit should replace the display. - `error`: Error message string or null. - Expose state and action handlers: - `inputDigit(digit: string)`: Handles digit buffering, prevents leading zero stacks, replaces buffer if `waitingForOperand` is true. - `inputDecimal()`: Guards against multiple decimals; prepends `'0.'` if waiting for operand. - `clear()`: Resets display to `'0'` (C behavior). - `allClear()`: Resets all state (AC behavior: display, accumulator, operation, error). - `toggleSign()`: Flips sign of current display value without NaN bugs. - `setBinaryOperation(op: BinaryOperation)`: Stores accumulator and operation; if chaining consecutively, computes intermediate local result before setting new operator. - `applyUnaryOperation(op: UnaryOperation)`: Immediately calculates unary transformation on the active display value locally (using IEEE 754 precision normalization) and updates display. - `evaluate()`: If binary operation and accumulator exist, computes the final result, updates display, clears pending operation, and sets `waitingForOperand = true`. Step 2: Implement comprehensive unit tests for the state machine in `web/src/hooks/useCalculator.test.ts`: - Use Vitest and `@testing-library/react`'s `renderHook` and `act`. - Test suites covering: - Digit input and buffering (e.g. typing `1`, `2`, `3` gives `'123'`). - Decimal guard (preventing `1.2.3`). - Sign toggling (`5` -> `-5` -> `5`, `0` -> `0`). - Clear (`C`) vs All Clear (`AC`). - Binary chaining (e.g. `5 + 3 = 8`, `5 + 3 * 2` local sequence). - Unary execution (e.g. `16` -> `sqrt` -> `4`, `50` -> `%` -> `0.5`). - Error boundaries (e.g. divide by zero local handling, negative sqrt). - Run `npm test` inside `web/` to confirm all hook tests pass. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not build UI components or commit yet. Pause and report the test execution results for review.
**Action Taken:** Implemented the `useCalculator` state machine hook with precision normalization, local evaluation, and error boundary handling. Created a comprehensive Vitest test suite using React Testing Library's `renderHook` to verify digit buffering, chained evaluations, unary transforms, and edge cases.

### Prompt 12
**Prompt:** Please execute this atomic step: 1. In `web/src/hooks/useCalculator.ts`, eliminate all `any` types in `catch` blocks (replace with `catch (err: unknown)` and narrow with `err instanceof Error ? err.message : 'UNKNOWN_ERROR'`) to maintain our zero-any policy. 2. Run `npm test` inside `web/` to confirm all 8 hook tests pass with 0 errors. 3. Task approved: stage and commit the hook, tests, and updated logs with an appropriate commit message. Pause and report back with git status once committed.
**Action Taken:** Eliminated all explicit `any` types from the `useCalculator` catch blocks, maintaining strict TypeScript safety. Verified 100% test passing rate and committed the state machine hook and unit tests.

### Prompt 13
**Prompt:** Please review `ARCHITECTURE.md` and `.agents/rules.md` to refresh architectural constraints (zero `any`, strict typing, component modularity). Next, implement the presentational UI components inside `web/src/components/` with Tailwind CSS styling: 1. `web/src/components/ThemeToggle.tsx`: Toggle button controlling dark/light mode. 2. `web/src/components/Display.tsx`: Displays current value and expression cleanly. 3. `web/src/components/Keypad.tsx`: Renders the keypad grid with varying styles. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not assemble `App.tsx` or run integration tests yet. Pause and report when components are created.
**Action Taken:** Authored the `ThemeToggle`, `Display`, and `Keypad` presentational React components inside `web/src/components/`. Used dynamic Tailwind CSS classes for premium styling (glassmorphism, micro-animations, dark mode) and properly passed strict types to all props.

### Prompt 14
**Prompt:** Please execute the following 3 atomic steps to complete Milestone 3: Step 1: Assemble the main layout in `web/src/App.tsx`: - Centered card layout matching our concept. Step 2: Add keyboard interaction: - Implement a global `keydown` event listener mapping keys to `useCalculator` actions. Step 3: Author UI interaction tests in `web/src/App.test.tsx` using Vitest and React Testing Library. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not commit yet. Pause and report back with the test results.
**Action Taken:** Assembled `App.tsx` integrating the UI components with the `useCalculator` hook and wired up comprehensive keyboard event listeners. Authored component integration tests verifying both mouse and keyboard interactions, resulting in a 100% test pass rate.

### Prompt 15
**Prompt:** Please execute this single atomic fix: 1. In `web/src/test/setup.ts`, make sure it imports `@testing-library/jest-dom/vitest` (or `@testing-library/jest-dom`). 2. In `web/tsconfig.test.json`, ensure both `"src/test/setup.ts"` and `"src/**/*.test.tsx"` are included, and add `"types": ["vitest/globals", "@testing-library/jest-dom"]` under `compilerOptions` so matchers like `toBeInTheDocument` are fully typed. 3. Verify with `npx tsc -p tsconfig.test.json --noEmit` inside `web/` that 0 type errors remain. 4. Task approved: stage and commit the components, assembly, tests, and configuration with: `feat(web): assemble calculator shell, keyboard listener, and ui interaction tests` Pause and report back with git status once committed.
**Action Taken:** Fixed testing pipeline types by configuring `setup.ts` and `tsconfig.test.json` to properly map `vitest` DOM matchers. Verified zero TypeScript errors and committed the completed UI components, `App.tsx` assembly, and full test suite.

### Prompt 16
**Prompt:** Please apply the following UX and styling polishes across the frontend workspace: 1. `web/src/hooks/useCalculator.ts`: Fix `toggleSign` with pure string manipulation and add expression tracking with post-evaluation reset behavior. 2. Button vs Screen convention: Use `xʸ` on button and `^` on display. 3. Visual styling and contrast in `web/src/App.tsx`. 4. Update unit tests in `useCalculator.test.ts` and `App.test.tsx` to verify the new expression lifecycle, and run `npm test`. Update logs and report test output without committing.
**Action Taken:** Refactored `useCalculator` to track the full math expression as a discrete state and modified `toggleSign` to use string manipulation to prevent precision artifacts. Updated styling in `App.tsx` for higher contrast. Rewrote relevant hooks and integration tests to verify expression lifecycle behaviors, successfully passing the 12-test suite.

### Prompt 17
**Prompt:** Please investigate and fix the theme styling regression across `web/`: 1. Inspect `web/tailwind.config.js` and `web/src/index.css`. 2. Re-implement the palette cleanly using standard CSS classes on `:root` and `.dark`. 3. Ensure `web/src/components/ThemeToggle.tsx` toggles the `dark` class on `document.documentElement`. 4. Check `web/src/App.tsx`, `Display.tsx`, and `Keypad.tsx` for standard Tailwind classes with `dark:` variants. 5. Run `npm test`. Update logs. Pause and report when completed.
**Action Taken:** Reverted CSS variables to standard Tailwind `dark:` mode utility classes with explicit hex codes to resolve a palette regression. Restored `darkMode: 'class'` in the Tailwind config and removed `data-theme` overrides in `ThemeToggle.tsx`. Applied base styling to the global body. Verified the 12-test suite continues to pass.

### Prompt 18
**Prompt:** Please apply a targeted palette and contrast overhaul across `web/src/` to strictly match the Realtime Colors specification. Then execute a final styling fix for light mode and finalize Milestone 3. Run `npm test`. Final task approval: Stage all working changes across `web/` and commit with `feat(web): complete milestone 3 - polished frontend shell, calculator state machine, and adaptive theme styling`. Update logs and report.
**Action Taken:** Implemented the exact Realtime Colors palette specification. Refined light mode with soft sage/teal backgrounds and crisp button borders. Successfully ran the full test suite and executed the milestone completion commit.

### Prompt 19
**Prompt:** Please execute this atomic bugfix: 1. In `web/src/hooks/useCalculator.ts`: Ensure `error` is automatically cleared whenever the user takes a valid subsequent action (`inputDigit`, `inputDecimal`, `toggleSign`, `setBinaryOperation`, `applyUnaryOperation`). 2. In `web/src/hooks/useCalculator.test.ts`: Add a unit test verifying this specific edge case. 3. Run `npm test`. 4. Task approved: stage and commit with: `fix(web): automatically dismiss error state on subsequent valid user input`. Update logs and report git status once committed.
**Action Taken:** Injected `setError(null)` guards across all valid action handlers within `useCalculator` to smoothly dismiss error states (e.g. `'INVALID_OPERAND'`). Added an explicit unit test verifying this UX enhancement. Tests passed (13/13) and changes committed.

### Prompt 20
**Prompt:** Please execute this atomic fix for unary operation expression handling: 1. In `web/src/hooks/useCalculator.ts`: In `applyUnaryOperation`, clear the completed expression to `""` and ensure `error` is cleared to `null`. Set `waitingForOperand = true` so subsequent digit inputs cleanly start a new number. 2. In `web/src/hooks/useCalculator.test.ts`: Add a test case verifying this lifecycle. 3. Run `npm test`. 4. Task approved: stage changes and commit with: `fix(web): reset stale expression when applying unary operations`. Update logs and report git status.
**Action Taken:** Updated the `applyUnaryOperation` function to correctly reset the visual math expression and ensure subsequent inputs properly overwrite the screen. Added an integration test specifically verifying this sequence (`9 × 9 =` followed by `sqrt`). The 14-test suite successfully passed, and the fix was committed.

### Prompt 21
**Prompt:** Please execute this atomic bugfix in `web/src/`: 1. In `web/src/hooks/useCalculator.ts`: Refine `applyUnaryOperation` to preserve the expression as `${accumulator} ${opSymbol}` if a binary operation is pending. 2. In `web/src/hooks/useCalculator.test.ts`: Add a unit test verifying this exact sequence (`50 + 20 % =`). 3. Run `npm test`. 4. Task approved: stage changes and commit with `fix(web): preserve pending binary expression when applying unary operation`. Update logs and report git status.
**Action Taken:** Refined the logic within `applyUnaryOperation` to inspect `operation` and `accumulator` before clearing the expression state. If an active equation exists, the visual string is retained so users aren't disoriented during compound operations like calculating percentages. Added an explicit test covering this flow, verifying that the 15-test suite continues to pass flawlessly.

### Prompt 22
**Prompt:** Please execute these two targeted refinements across `web/src/`: Step 1: Swap the layout and styling of `=` and `xʸ` in `web/src/components/Keypad.tsx`. Step 2: Implement single-character backspace in `web/src/hooks/useCalculator.ts` and map it to `'Backspace'` in `web/src/App.tsx`. Step 3: Add unit tests for `backspace()`. Run `npm test`. Task approved: stage all changes and commit with `feat(web): swap equals with power key and implement single-digit keyboard backspace`. Update logs.
**Action Taken:** Swapped the keypad positioning and styling for `=` and `xʸ` to establish a dedicated, full-width evaluation bar at the bottom. Implemented a robust single-character backspace function that intelligently handles negative numbers and evaluation states, replacing the heavy-handed `clear()` mapping on the Backspace key. Tests passed (18/18) and changes committed.

### Prompt 23
**Prompt:** We are beginning Milestone 4: Client-to-Server Integration. 1. Pre-flight check: - Read `.agents/rules/rules.md` and `ARCHITECTURE.md` to refresh all architectural constraints (strict TypeScript, zero `any`, domain-driven separation, error contracts). - Review `.agents/logs/` (today's log) to understand the current state of the application. 2. Execute Step 1 of Milestone 4 (API Service & Dev Proxy): - In `web/vite.config.ts`: configure the dev proxy to route `/api` to `http://localhost:3000` with `changeOrigin: true`. - Create `web/src/services/calculatorApi.ts`: - Define strict TypeScript request/response interfaces matching the backend contracts: - Binary: `operation` (`'add' | 'subtract' | 'multiply' | 'divide' | 'power'`), `a: number`, `b: number`. - Unary: `operation` (`'sqrt' | 'percentage'`), `value: number`. - Implement typed client functions: - `calculateBinary(payload: BinaryCalculationRequest): Promise<CalculationResponse>` - `calculateUnary(payload: UnaryCalculationRequest): Promise<CalculationResponse>` - Handle non-2xx HTTP responses by extracting server error codes/messages or throwing a typed custom error class (`CalculatorApiError`). - Zero `any` policy (use `unknown` and type narrowing). - Create unit tests in `web/src/services/calculatorApi.test.ts`: - Mock global `fetch` via `vi.fn()`. - Test cases: successful binary call, successful unary call, 400 error parsing (e.g. `DIVIDE_BY_ZERO`), and network failure handling. - Run `npm test` inside `web/` to ensure all tests pass. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not wire the service into `useCalculator` or commit yet. Pause and report the test runner output.
**Action Taken:** Read architectural constraints, logs, and rules. Configured Vite API proxy targeting `http://localhost:3000`. Created strict-typed API client service `calculatorApi.ts` matching backend JSON contracts, zero explicit `any` usage, and custom error handling logic. Authored fetch-mocked unit tests and confirmed they run successfully without errors.

### Prompt 24
**Prompt:** Please execute Step 2 of Milestone 4: Wire the API Service into `useCalculator`: 1. Update `web/src/hooks/useCalculator.ts`: - Import `calculateBinary` and `calculateUnary` from `../services/calculatorApi`. - Add an `isLoading: boolean` state flag (default `false`) and expose it in the hook's return object. - Refactor `evaluate` to be an async function... - Refactor `applyUnaryOperation` to be async... - Guard against concurrent submissions: do not trigger API evaluations if `isLoading` is already true. 2. Update tests in `web/src/hooks/useCalculator.test.ts`: - Mock `../services/calculatorApi` (or use `vi.spyOn` on the service methods) so unit tests do not hit network sockets. - Update hook test calls that trigger `evaluate` or `applyUnaryOperation` to handle promises properly with `await act(async () => { ... })`. - Add a test verifying `isLoading` toggles during network resolution. - Add a test verifying server error responses (e.g. `DIVIDE_BY_ZERO`) properly populate the hook's `error` state. 3. Run `npm test` inside `web/` to verify all hook and API tests pass. Update `PROMPT_LOG.md` and `.agents/logs/`. Do not commit yet. Pause and report the test runner output.
**Action Taken:** Wired the `calculatorApi` service into the `useCalculator` state machine. Refactored `evaluate`, `applyUnaryOperation`, and `setBinaryOperation` to handle async API calls with `isLoading` guards and error state mapping. Updated the hook test suite to mock network responses and successfully verified the new async behaviors.

### Prompt 25
**Prompt:** Please execute Step 3 of Milestone 4: UI Loading State & Full Integration Testing: 1. Connect `isLoading` to the UI: - In `web/src/App.tsx`: - Pass `isLoading` from `useCalculator()` down into `Display.tsx` and `Keypad.tsx`. - In `web/src/components/Display.tsx`: - Render a subtle loading indicator (e.g. a small pulsing dot or spinner in the top corner near the expression area) when `isLoading` is true. Ensure it does not shift or distort the expression or main display line. - In `web/src/components/Keypad.tsx`: - Optionally apply `disabled={isLoading}` and a slight opacity transition (e.g., `opacity-80 cursor-not-allowed`) to prevent ghost clicks during network calls. 2. Comprehensive Integration Tests in `web/src/App.test.tsx`: - Write full end-to-end integration tests mocking `calculatorApi`: - Round-trip binary calculation: click `5`, click `+`, click `3`, click `=`, wait for the result to resolve, assert display updates to `'8'`. - Round-trip unary calculation: click `9`, click `√`, wait for the result, assert display updates to `'3'`. - Server error feedback: mock an API rejection returning `{ error: 'DIVIDE_BY_ZERO' }` when dividing by `0`, assert the error badge/text appears on the screen. - Recovery test: following the error display, click `AC` or a digit and verify the error state clears cleanly. 3. Run the full test suite: - Run `npm test` inside `web/` to verify all unit and integration tests pass. - Run `npm run lint` or typecheck if configured. Update `PROMPT_LOG.md` and `.agents/logs/`. Pause and report the test runner output before committing.
**Action Taken:** Connected `isLoading` state to the UI to render a pulsing indicator in the `Display` component and disable all `Keypad` buttons to prevent concurrent interaction. Authored full UI integration tests verifying async loading, mock responses, error parsing, and graceful error recovery. Verified 100% test success across the workspace.

### Prompt 26
**Prompt:** Please apply these targeted backend-alignment fixes in `web/`: 1. Update the Dev Proxy Port: - In `web/vite.config.ts`, update the proxy target for `/api` from `http://localhost:3000` to `http://localhost:3001` to match the Express backend server port. 2. Align Unary Payload Contract: - In `web/src/services/calculatorApi.ts`, inspect `calculateUnary`: - Ensure the POST request body sends `{ a: payload.value }` (or `{ a: ... }`) instead of `{ value: ... }` to match the backend's `unaryOperationSchema` which expects `{ a }`. - Ensure the return format matches the backend response `{ result: number }`, computing or assigning `formatted` consistently without type coercion errors. 3. Verify Tests: - Update any unit tests in `web/src/services/calculatorApi.test.ts` that assert the unary payload body so they expect `{ a: ... }`. - Run `npm test` inside `web/` to confirm all 26 tests pass. Update PROMPT_LOG.md and .agents/logs/. Pause and report the test results before committing.
**Action Taken:** Updated the Vite dev proxy target to port 3001. Verified that `calculatorApi.ts` and its test file already correctly conform to the unary `{ a: payload.value }` contract. Ran tests to verify 100% success.

### Prompt 27
**Prompt:** Please simplify the response handling in `web/src/services/calculatorApi.ts`: 1. Update the interface: - In `CalculationResponse`, remove unused fields so it cleanly reflects what the frontend needs (`result` and `formatted`). 2. Refactor `handleResponse`: - Replace the verbose manual `typeof` runtime checks with a clean, idiomatic response parser. 3. Verify: - Run `npm test` inside `web/` to make sure all 26 tests pass. Update `PROMPT_LOG.md` and `.agents/logs/`. Pause and report the test results before committing.
**Action Taken:** Simplified the `CalculationResponse` interface to remove the unused `operation` field. Refactored the `handleResponse` function in `calculatorApi.ts` to use clean, idiomatic parsing instead of verbose runtime type checks. Updated the unit tests in `calculatorApi.test.ts` to expect the simplified response object and successfully re-ran the entire test suite.

### Prompt 28
**Prompt:** In `web/src/App.tsx`: 1. In `handleKeyDown`, add an early return guard at the very beginning: `if (isLoading) return;`. Add `isLoading` to the dependency array of the useEffect handling keydown. In Display.tsx, ensure the loading indicator element has absolute positioning so it cannot trigger layout shifts. Run npm test in web/ to confirm all 26 tests still pass. Update PROMPT_LOG.md and .agents/logs/. Pause and report back.
**Action Taken:** Added early return guard for `isLoading` in keyboard event listener and updated the dependency array. Verified `Display.tsx` already uses `absolute` positioning for the loading indicator to prevent layout shifts. Re-ran tests to confirm 100% success.

### Prompt 29
**Prompt:** Please execute Step 1 of Milestone 5: Containerize Backend and Frontend: 1. Create `api/Dockerfile`: - Use a lightweight multi-stage Node build. - Stage 1 (builder): Install dependencies, build TypeScript. - Stage 2 (runner): Install prod dependencies, copy assets, expose 3001, define start command. 2. Create `web/Dockerfile`: - Use multi-stage build. - Stage 1 (builder): Install dependencies, build Vite bundle. - Stage 2 (runner): Use minimal Alpine Nginx image, serve static assets and proxy `/api` to `api:3001`. 3. Create root `docker-compose.yml`: - Define `api` and `web` services, set up networking, expose `web` on port 8080. 4. Verify local configuration: - Create `.dockerignore` in both folders. Update `PROMPT_LOG.md` and `.agents/logs/`. Pause and report back.
**Action Taken:** Added `start` script to `api/package.json`. Created `api/Dockerfile` (Node multi-stage) and `web/Dockerfile` (Node + Nginx multi-stage). Configured `web/nginx.conf` for serving assets and proxying `/api` to the backend. Authored root `docker-compose.yml` to orchestrate the services and created `.dockerignore` files for cleanly built contexts.
